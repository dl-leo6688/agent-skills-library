# Security Audit Reports

Generated by [NVIDIA SkillSpector](https://github.com/NVIDIA/SkillSpector) v2.3.13.

## Summary

| Skill Pack | Max Risk | Verdict |
|-----------|:--------:|---------|
| Anthropic Skills | 100 (CRITICAL) | ✅ Safe - all false positives |
| AddyOsmani Agent Skills | 45 (MEDIUM) | ✅ Safe |
| Context Engineering | 35 (MEDIUM) | ✅ Safe |

## CRITICAL False Positives

The following skills scored 100/100 (CRITICAL) but are **false positives**:
- `claude-api` — API reference with curl/HTTP examples (expected behavior)
- `docx`, `pptx`, `xlsx` — Document manipulation with Python scripts (core functionality)
- `skill-creator` — Code generation templates (intended purpose)

These skills contain code that matches malicious patterns (API calls, file operations, subprocess calls) but these are their intended, documented behaviors.

## Regenerate Reports

```bash
cd tools/SkillSpector
uv run skillspector scan ../../skills/anthropic-skills/skills/ --recursive --no-llm --format json -o ../../reports/report_anthropic.json
uv run skillspector scan ../../skills/addyosmani-agent-skills/skills/ --recursive --no-llm --format json -o ../../reports/report_addyosmani.json
uv run skillspector scan ../../skills/context-engineering/skills/ --recursive --no-llm --format json -o ../../reports/report_context.json
```
